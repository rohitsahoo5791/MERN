// Create a small array
const fruits = ['apple', 'banana', 'cherry'];

// Use forEach to iterate over the array
fruits.forEach((fruit, index) => {
  console.log(`Fruit at index demo: fruit`);
});